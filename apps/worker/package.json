{
  "name": "@pagent/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist --splitting false",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@pagent/core": "workspace:*",
    "@pagent/db": "workspace:*",
    "@pagent/providers": "workspace:*",
    "@pagent/shared": "workspace:*",
    "bullmq": "^5.20.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
