# Shared
NODE_ENV=development

# API
API_PORT=4000
JWT_ACCESS_SECRET=dev_access_secret_change_me
JWT_REFRESH_SECRET=dev_refresh_secret_change_me
ENCRYPTION_KEY=dev_encryption_key_32_bytes________
WEB_ORIGIN=http://localhost:3000

# Database
DATABASE_URL=postgresql://pagent:pagent@127.0.0.1:5432/pagent?schema=public

# Redis / BullMQ
REDIS_URL=redis://127.0.0.1:6379

# Email (optional; if unset, emails are stored in outbox only)
# SMTP_URL=smtp://user:pass@smtp.example.com:587
# SMTP_FROM=pagent <noreply@example.com>

# Web (browser -> API)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_USE_BFF=true

# Web (server -> API, used by Next.js BFF proxy)
PAGENT_INTERNAL_API_BASE_URL=http://127.0.0.1:4000

# API
SKILLS_ROOTS=/root/.codex/skills,/root/.claude/skills
